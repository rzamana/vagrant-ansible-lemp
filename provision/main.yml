---
- name: Install stuff, configure sites & DB's
  hosts: all
  become: yes
  become_user: root
  become_method: sudo
  roles:
    - studioboz
    - common/timezone
    - common/base-packages
    - nginx
    - tools
